# Anthropic / Claude
ANTHROPIC_API_KEY=

# Alfred transport: "messages" (default) or "managed-agents"
ALFRED_TRANSPORT=messages
ALLOW_TRANSPORT_FALLBACK=false

# Local server
PORT=3001

# Data directory (defaults to ~/.alfred)
ALFRED_HOME=

# Debug mode (set to "1" for verbose logging)
ALFRED_DEBUG=
